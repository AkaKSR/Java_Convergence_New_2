-- ========================================= --
SELECT
	*
FROM
	EMPLOYEES;
-- ========================================= --

-- 11. LAST_NAME에 a및 e가 들어가 있는 사우너들의 LAST_NAME을 조회
SELECT
	LAST_NAME
FROM
	EMPLOYEES
WHERE